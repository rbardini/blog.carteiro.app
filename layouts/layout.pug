doctype html
html(lang="pt-BR")
  head
    meta(charset="UTF-8")
    block title
    meta(name="author", content=site.author )
    meta(name="viewport", content="width=device-width, initial-scale=1")
    meta(name="theme-color", content="#ffc107")
    meta(name="turbolinks-cache-control", content="no-preview")
    link(rel="alternate", href="/rss.xml", type="application/rss+xml", title="RSS")
    link(rel="stylesheet", href="//fonts.googleapis.com/css?family=Roboto:300|Roboto+Slab")
    link(rel="stylesheet", href="/css/main.min.css")

  body
    #content
      header(role="banner")
        h1
          a(href="/", rel="home")
            img(src="/img/logo.png" class="logo" alt="Logo")

      main(role="main")
        block content

      aside(role="complementary")
        p.
          Nunca mais perca uma entrega ou esqueça de buscar seu pacote.
          O Carteiro lhe avisa como você quiser, quando você precisar.

      footer(role="contentinfo")
        p.
          Feito com #[span(class="❤") ❤] desde 2012 #[br]
          #[a(href=site.storeUrl) Carteiro no Google Play] · #[a(href="/arquivo") Arquivo] · #[a(href="/rss.xml") RSS]

    script(src="/js/turbolinks.js", data-turbolinks-eval="false")
    if development
      script(src="http://localhost:35729/livereload.js", data-turbolinks-eval="false")
    unless development
      script(data-turbolinks-eval="false").
        window.ga=window.ga||function(){(ga.q=ga.q||[]).push(arguments)};ga.l=+new Date;
        ga('create','#[=site.trackingId]','auto');ga('send','pageview');
      script(async, src='https://www.google-analytics.com/analytics.js', data-turbolinks-eval="false")
